class Solution: 
    def Merge(self, arr,left,mid,right):
        
        temp=[]
        low=left
        high=mid+1



    def mergesort(self,arr,low,high):
      if low>=high:
        return
      mid=low+high // 2
      self.merge(self,arr,low,mid


sol=Solution()
arr=[10,34,5,6,2,56]
n=len(arr)
i=0
arr_result=sol.InsertionSort(arr,n,i)
print(arr_result)

